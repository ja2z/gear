/**
 * Static upcoming events for the mobile hub hero cards.
 * The desktop dashboard fetches real events from /api/events instead.
 * Empty array hides the hero cards until entries are added or mobile is wired to the API.
 */
export const UPCOMING_EVENTS = [];
